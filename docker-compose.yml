services:
  app:
    build: .
    image: budget-redo:latest
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - ROOT_URL=http://paloma.local:3000
      - MONGO_URL=mongodb://mongo:27017/budget
      - PORT=3000
    depends_on:
      mongo:
        condition: service_started
    restart: unless-stopped

  mongo:
    image: mongo:7
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
